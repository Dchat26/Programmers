#include <stdio.h>

int main() {
	int n = 0;
	if (scanf("%d", &n) != 1) return 0;

	for (int i = 1; i <= n * n; i++) {
		printf("%d", i);

		if (i % n == 0) printf("\n");
		else printf(" ");
	}

	return 0;
}
